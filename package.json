{
  "name": "log_replayer",
  "version": "0.0.1",
  "description": "Replays a command log on an initial state.",
  "main": "log_replayer.js",
  "scripts": {
    "test": "node tests/test.js && node tests/custom_formats.js"
  },
  "author": "Matthew Palmer",
  "license": "ISC",
  "dependencies": {
    "safe-access": "^0.1.0"
  }
}
